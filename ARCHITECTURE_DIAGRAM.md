# Enhanced GlobalTimeline - Architecture Diagram

## Component Hierarchy

```
App
├── SessionProvider
│   ├── ToastProvider
│   │   ├── MeetingTimerProvider ← NEW
│   │   │   └── AppContent
│   │   │       ├── GlobalTimeline ← ENHANCED
│   │   │       │   ├── PhaseMarker (x8)
│   │   │       │   ├── Timeline Bar
│   │   │       │   │   ├── Red Fill Bar (elapsed)
│   │   │       │   │   └── Blue Thumb (current phase)
│   │   │       │   └── Controls
│   │   │       │       ├── Pause/Resume Button
│   │   │       │       └── Reset Button
│   │   │       ├── PhaseRouter
│   │   │       │   ├── WelcomePage
│   │   │       │   ├── CustomerDiscoveryPage
│   │   │       │   ├── DivergePage
│   │   │       │   ├── ConvergePage
│   │   │       │   ├── SpotExercisesPage
│   │   │       │   ├── PrioritizationPage
│   │   │       │   ├── SynthesisReviewPage
│   │   │       │   └── BriefCompletePage
│   │   │       ├── ToastContainer
│   │   │       └── DevTools
```

## Context Flow

```
┌─────────────────────────────────────────────────────────────┐
│                     SessionContext                           │
│  • state: SessionState                                       │
│  • currentPhase: SessionPhase                                │
│  • completedPhases: SessionPhase[]                          │
│  • navigateToPhase(phase) → boolean                         │
│  • setPhase(phase)                                           │
│  • nextPhase()                                               │
└─────────────────────────────────────────────────────────────┘
                            ↓
                    Provides to all components
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                   MeetingTimerContext                        │
│  • timerState: MeetingTimerState                            │
│    - meetingStartTime: number | null                         │
│    - totalElapsedMs: number                                  │
│    - expectedDurationMs: number                              │
│    - isPaused: boolean                                       │
│    - phaseTimestamps: Record<Phase, PhaseTimestamp>         │
│  • startMeeting(duration)                                    │
│  • pauseMeeting()                                            │
│  • resumeMeeting()                                           │
│  • resetMeeting()                                            │
│  • enterPhase(phase)                                         │
│  • exitPhase(phase)                                          │
│  • getCurrentElapsedMs() → number                           │
│  • getPhaseElapsedMs(phase) → number                        │
│  • getPacingStatus() → 'on-track'|'behind'|'over'          │
└─────────────────────────────────────────────────────────────┘
                            ↓
                    Used by GlobalTimeline
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                      ToastContext                            │
│  • toasts: Toast[]                                           │
│  • addToast(message, type, duration)                        │
│  • removeToast(id)                                           │
└─────────────────────────────────────────────────────────────┘
```

## Data Flow - Timer Updates

```
1. Timer Initialization
   ┌──────────────────┐
   │  App Starts      │
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ MeetingTimerProvider initializes
   │ • Loads from localStorage
   │ • Or creates default state
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ GlobalTimeline renders
   │ • Checks if timer started
   │ • If not: startMeeting(duration)
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ Timer Active     │
   │ meetingStartTime = Date.now()
   └──────────────────┘

2. Timer Updates (Every Second)
   ┌──────────────────┐
   │ setInterval(1000)│
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ Calculate Elapsed│
   │ elapsed = now - start - paused
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ Update State     │
   │ totalElapsedMs = elapsed
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ UI Re-renders    │
   │ • Timer display updates
   │ • Red bar grows
   │ • Pacing color changes
   └──────────────────┘

3. Persistence (Every 10 Seconds)
   ┌──────────────────┐
   │ setInterval(10000)│
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ Save to localStorage
   │ key: 'creative-discovery-meeting-timer'
   └──────────────────┘
```

## Data Flow - Phase Navigation

```
1. User Clicks Phase Marker
   ┌──────────────────┐
   │ User clicks "Converge" marker
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ handlePhaseClick()│
   │ targetPhase = 'sticky-notes-converge'
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ Progressive Navigation Check
   │ • currentIndex = 2 (Diverge)
   │ • targetIndex = 3 (Converge)
   │ • targetIndex === currentIndex + 1? ✓
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ SessionContext.setPhase()
   │ currentPhase = 'sticky-notes-converge'
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ MeetingTimer tracks phase
   │ • exitPhase('sticky-notes-diverge')
   │ • enterPhase('sticky-notes-converge')
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ UI Updates       │
   │ • Blue thumb moves
   │ • Previous phase gets checkmark
   │ • New phase highlighted
   │ • PhaseRouter changes view
   └──────────────────┘

2. User Tries to Skip Ahead
   ┌──────────────────┐
   │ User clicks "Review" marker
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ handlePhaseClick()│
   │ targetPhase = 'synthesis-review'
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ Progressive Navigation Check
   │ • currentIndex = 2 (Diverge)
   │ • targetIndex = 6 (Review)
   │ • targetIndex > currentIndex + 1? ✗
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ Navigation Blocked│
   │ addToast('Complete current phase first')
   └────────┬─────────┘
            │
            ▼
   ┌──────────────────┐
   │ Toast Appears    │
   │ No phase change
   └──────────────────┘
```

## Visual Element Rendering

```
┌─────────────────────────────────────────────────────────────┐
│                    GlobalTimeline                            │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  Header Section                                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ Meeting Time: 15:30 / 60:00        [⏸] [Reset]      │  │
│  │             [PAUSED]                                  │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  Phase Markers                                                │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  ✓    ✓    ◉    ○    🔒   🔒   🔒   🔒              │  │
│  │  W    D   Dv    C    SE   P    R    B               │  │
│  │  │    │    │    │    │    │    │    │               │  │
│  │  └────┴────┴────┴────┴────┴────┴────┘               │  │
│  │  Completed  Current  Locked phases                    │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  Timeline Bar                                                 │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ ▓▓▓▓▓▓▓▓░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░   │  │
│  │      ◉                                                 │  │
│  │   Red Fill    Blue Thumb                              │  │
│  │   (elapsed)   (current phase)                         │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  Legend                                                       │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  [▓] Time Elapsed    [◉] Current Phase               │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
│  Footer                                                       │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ Current Phase: Diverge          Progress: 3 of 8     │  │
│  └───────────────────────────────────────────────────────┘  │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

## State Management Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     localStorage                             │
│                                                               │
│  Key: 'creative-discovery-session'                          │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ SessionState {                                       │   │
│  │   sessionId: string                                  │   │
│  │   currentPhase: SessionPhase                        │   │
│  │   completedPhases: SessionPhase[] ← NEW             │   │
│  │   projectContext: { ... }                           │   │
│  │   customerDiscovery: { ... }                        │   │
│  │   stickyNoteExercise: { ... }                       │   │
│  │   spotExercises: { ... }                            │   │
│  │   prioritization: { ... }                           │   │
│  │   creativeBrief: { ... }                            │   │
│  │ }                                                    │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                               │
│  Key: 'creative-discovery-meeting-timer' ← NEW              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ MeetingTimerState {                                  │   │
│  │   meetingStartTime: 1704398400000                   │   │
│  │   totalElapsedMs: 1800000                           │   │
│  │   expectedDurationMs: 3600000                       │   │
│  │   isPaused: false                                    │   │
│  │   pausedAt: null                                     │   │
│  │   totalPausedMs: 120000                             │   │
│  │   phaseTimestamps: {                                │   │
│  │     'project-context': {                            │   │
│  │       enteredAt: 1704398400000                      │   │
│  │       exitedAt: 1704398700000                       │   │
│  │       totalTimeMs: 300000                           │   │
│  │     },                                               │   │
│  │     'customer-discovery': {                         │   │
│  │       enteredAt: 1704398700000                      │   │
│  │       totalTimeMs: 600000                           │   │
│  │     }                                                │   │
│  │   }                                                  │   │
│  │ }                                                    │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                            ↕
                    Auto-save every 10s
                            ↕
┌─────────────────────────────────────────────────────────────┐
│                    React State (Memory)                      │
│                                                               │
│  SessionContext                                              │
│  • SessionState                                              │
│  • Methods: navigateToPhase, setPhase, etc.                 │
│                                                               │
│  MeetingTimerContext                                         │
│  • MeetingTimerState                                         │
│  • Methods: pauseMeeting, resumeMeeting, etc.               │
│                                                               │
│  ToastContext                                                │
│  • Toast[]                                                   │
│  • Methods: addToast, removeToast                           │
└─────────────────────────────────────────────────────────────┘
```

## Progressive Navigation Logic

```
Current Phase Index: 2 (Diverge)

┌──────────────────────────────────────────────────────────┐
│  Phase Navigation Matrix                                  │
├──────────────────────────────────────────────────────────┤
│  Target Phase          │ Index │ Can Navigate? │ Reason  │
├────────────────────────┼───────┼───────────────┼─────────┤
│  project-context       │   0   │      ✓       │ Backward│
│  customer-discovery    │   1   │      ✓       │ Backward│
│  sticky-notes-diverge  │   2   │      ✓       │ Current │
│  sticky-notes-converge │   3   │      ✓       │ Next    │
│  spot-exercises        │   4   │      ✗       │ Skip 2+ │
│  prioritization        │   5   │      ✗       │ Skip 2+ │
│  synthesis-review      │   6   │      ✗       │ Skip 2+ │
│  brief-complete        │   7   │      ✗       │ Skip 2+ │
└──────────────────────────────────────────────────────────┘

Algorithm:
  if (targetIndex <= currentIndex) return true;  // Backward
  if (targetIndex === currentIndex + 1) return true;  // Next
  return false;  // Skip ahead blocked
```

## Pacing Calculation

```
┌─────────────────────────────────────────────────────────────┐
│                  Pacing Status Algorithm                     │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  Input:                                                       │
│  • currentPhaseIndex = 3 (Converge)                         │
│  • totalPhases = 8                                           │
│  • elapsed = 2,100,000 ms (35 min)                          │
│  • expected = 3,600,000 ms (60 min)                         │
│                                                               │
│  Step 1: Calculate expected progress                         │
│  progressRatio = 3 / 8 = 0.375 (37.5%)                      │
│                                                               │
│  Step 2: Calculate expected elapsed at this point            │
│  expectedElapsed = 3,600,000 × 0.375 = 1,350,000 ms (22.5m)│
│                                                               │
│  Step 3: Compare actual vs expected                          │
│  actualRatio = 2,100,000 / 1,350,000 = 1.56 (156%)         │
│                                                               │
│  Step 4: Determine status                                    │
│  if (actualRatio > 1.30) → 'over' ✓                        │
│  if (actualRatio > 1.10) → 'behind'                         │
│  else → 'on-track'                                           │
│                                                               │
│  Result: 'over' (significantly behind schedule)              │
│  Background Color: Red (#dc2626)                             │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

## Timeline Position Calculations

```
┌─────────────────────────────────────────────────────────────┐
│              Timeline Position Calculations                  │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  Phase Durations (60-minute workshop):                       │
│  ┌───────┬──────────┬─────────┬────────────────┐           │
│  │ Phase │ Duration │ Cumul.  │ Position (%)   │           │
│  ├───────┼──────────┼─────────┼────────────────┤           │
│  │ W     │  5 min   │  0 min  │   0.0%         │           │
│  │ D     │ 15 min   │  5 min  │   8.3%         │           │
│  │ Dv    │ 10 min   │ 20 min  │  33.3%         │           │
│  │ C     │ 10 min   │ 30 min  │  50.0%  ← Red bar here     │
│  │ SE    │ 10 min   │ 40 min  │  66.7%    Blue thumb here │
│  │ P     │  5 min   │ 50 min  │  83.3%         │           │
│  │ R     │  3 min   │ 55 min  │  91.7%         │           │
│  │ B     │  2 min   │ 58 min  │  96.7%         │           │
│  └───────┴──────────┴─────────┴────────────────┘           │
│                                                               │
│  Example at 30 minutes elapsed, on phase "Exercises":        │
│  • Red Fill Bar: 50% (30/60 min)                            │
│  • Blue Thumb: 66.7% (at SE phase marker)                   │
│  • Gap shows user is ahead of time!                          │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

## Hook Dependencies

```
┌──────────────────────┐
│  useMeetingTimer     │
│  (Context Consumer)  │
└──────┬───────────────┘
       │
       │ Returns:
       │ • timerState
       │ • startMeeting()
       │ • pauseMeeting()
       │ • resumeMeeting()
       │ • resetMeeting()
       │ • enterPhase()
       │ • exitPhase()
       │ • getCurrentElapsedMs()
       │ • getPhaseElapsedMs()
       │ • getPacingStatus()
       │
       └──────────┐
                  ▼
       ┌──────────────────────┐
       │  useFormattedTime    │
       │  (Utility Hook)      │
       └──────┬───────────────┘
              │
              │ Uses:
              │ • useMeetingTimer()
              │
              │ Returns:
              │ • formatMs()
              │ • formatMsLong()
              │ • getCurrentElapsedFormatted()
              │ • getExpectedDurationFormatted()
              │ • getProgressPercentage()
              │
              └──────────┐
                         ▼
              ┌──────────────────────┐
              │  usePhaseTimer       │
              │  (Lifecycle Hook)    │
              └──────┬───────────────┘
                     │
                     │ Uses:
                     │ • useMeetingTimer()
                     │
                     │ Effect:
                     │ • Tracks phase entry/exit
                     │ • Auto-updates timestamps
                     │ • Cleanup on unmount
```

## Error Handling Flow

```
┌─────────────────────────────────────────────────────────────┐
│                  Error Handling Strategy                     │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  localStorage Operations:                                    │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ try {                                                  │ │
│  │   localStorage.setItem(key, value);                   │ │
│  │ } catch (error) {                                     │ │
│  │   if (error.name === 'QuotaExceededError') {         │ │
│  │     console.error('Storage quota exceeded');         │ │
│  │     // Fallback: Keep in memory only                 │ │
│  │   } else {                                            │ │
│  │     console.error('localStorage failed:', error);    │ │
│  │     // Continue without persistence                   │ │
│  │   }                                                    │ │
│  │ }                                                      │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                               │
│  Context Consumer:                                           │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ const context = useContext(MeetingTimerContext);      │ │
│  │ if (!context) {                                        │ │
│  │   throw new Error('Must be within Provider');        │ │
│  │ }                                                      │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                               │
│  Navigation Errors:                                          │
│  ┌───────────────────────────────────────────────────────┐ │
│  │ const success = navigateToPhase(target);              │ │
│  │ if (!success) {                                        │ │
│  │   addToast('Navigation blocked', 'warning');         │ │
│  │   return;                                             │ │
│  │ }                                                      │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

## Performance Optimization

```
┌─────────────────────────────────────────────────────────────┐
│                Performance Optimizations                     │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  1. Memoization                                              │
│     • useMemo for phase configs calculation                 │
│     • useMemo for position calculations                     │
│     • useCallback for event handlers                        │
│                                                               │
│  2. Update Throttling                                        │
│     • Display updates: 1 second intervals                   │
│     • Persistence: 10 second intervals                      │
│     • No updates when paused                                │
│                                                               │
│  3. Selective Re-renders                                     │
│     • Context split (Session, Toast, Timer)                 │
│     • Only subscribe to needed values                       │
│     • React.memo for pure components                        │
│                                                               │
│  4. Lazy Calculations                                        │
│     • Calculate elapsed only when needed                    │
│     • Phase positions cached                                │
│     • Tooltip content computed on hover                     │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```
